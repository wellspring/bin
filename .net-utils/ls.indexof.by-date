#!/bin/sh
# List the content of `index of` html pages (generated by servers when no index file present), sorted by date.
#   Usage: ls.indexof <url...>
#     e.g. ls.indexof https://dumps.wikimedia.org/enwiki/latest/ https://dumps.wikimedia.org/ruwiki/latest/ 
ls.indexof "$@" | perl -M'Date::Parse' -F'\t' -lane 'printf str2time($F[1]); print " ".$_' | sort -nk1 | sed 's/^[0-9]\+ //' | align
