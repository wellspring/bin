#!/bin/sh
# List the content of `index of` html pages (generated by servers when no index file present), sorted by size.
#   Usage: ls.indexof <url...>
#     e.g. ls.indexof https://dumps.wikimedia.org/enwiki/latest/ https://dumps.wikimedia.org/ruwiki/latest/ 
ls.indexof "$@" | sort -hk4 | align
